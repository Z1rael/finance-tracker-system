syntax = "proto3";

package finance.transaction;

option go_package = "gen/go/transaction;transaction";

import "google/protobuf/timestamp.proto";
import "google/protobuf/empty.proto";
import "proto/common.proto";

// Models

message Transaction {
    int64 id                            = 1;
    int64 account_id                    = 2;
    double amount                       = 3;
    string description                  = 4;
    finance.common.TransactionCategory category = 5;
    finance.common.TransactionType transaction_type = 6;
    google.protobuf.Timestamp timestamp = 7;
}

// Requests / Responses

message CreateTransactionRequest {
    int64 account_id                    = 1;
    double amount                       = 2;
    string description                  = 3;
    finance.common.TransactionCategory category = 4;
    finance.common.TransactionType transaction_type = 5;
    google.protobuf.Timestamp timestamp = 6;
}

message CreateTransactionResponse {
    Transaction transaction = 1;
}

message ListTransactionsRequest {
    int64 account_id                     = 1;
    google.protobuf.Timestamp start_time = 2;
    google.protobuf.Timestamp end_time   = 3;
}

message ListTransactionsResponse {
    repeated Transaction transactions = 1;
}

message DeleteTransactionRequest {
    int64 id = 1;
}

// Service

service TransactionService {
    rpc CreateTransaction (CreateTransactionRequest) returns (CreateTransactionResponse);
    rpc ListTransactions (ListTransactionsRequest) returns (ListTransactionsResponse);
    rpc DeleteTransaction (DeleteTransactionRequest) returns (google.protobuf.Empty);
}